from math import *

a = float(input())
b = float(input())
c = float(input())

D = (b * b) - (4 * a * c)

if D < 0:
    print('Нет корней')
elif D == 0:
    print(-b / (2 * a))
else:
    print(min((-b + sqrt(D)) / (2 * a),((-b - sqrt(D)) / (2 * a))))
    print(max((-b + sqrt(D)) / (2 * a),((-b - sqrt(D)) / (2 * a))))

    